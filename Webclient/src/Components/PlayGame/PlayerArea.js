import React, { Component } from "react";
import PropTypes from "prop-types"

class PlayerArea extends Component {
  static propTypes = {
  };

  render() {
    return <div className="playerArea__container"> &nbsp; </div>;
  }
}

export default PlayerArea;
